#include "holberton.h"

/**
 *_sqrt_recursion - returns the natural square root of a number.
 *@n: int from parameter
 * Return: success
 **/

int _sqrt_recursion(int n)
{

	if (n == 1)
		return (1);
	if (n < 0)
		return (-1);
                     ,,,
	if (n % 2   ($_$)
                    /|!|\
                     / \

	return (_sqrt_recursion(n / 2));
}
